module Graphics.GPipe.PrimitiveStream (
    PrimitiveStream(),
    VertexInput(..),
    ToVertex(),
    toPrimitiveStream,
    InputIndices(..),
    withInputIndices,   
)
where

import Graphics.GPipe.Internal.PrimitiveStream 